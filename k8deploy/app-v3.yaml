apiVersion: apps/v1
kind: Deployment
metadata:
  name: trendtrove-app-v3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trendtrove-app-v3
  template:
    metadata:
      labels:
        app: trendtrove-app-v3
    spec:
      containers:
        - name: app
          image: us-central1-docker.pkg.dev/canaryguard-bda/canaryguard/trendtrove-app:v3
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: trendtrove-secrets
            - configMapRef:
                name: trendtrove-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          env:
            - name: FAIL_MODE
              value: "cart"
            - name: SERVICE_NAME
              value: app-service-v3
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://otel-collector:4318/v1/traces
            - name: AUTH_SERVICE_URL
              value: http://auth-service:3000
            - name: PRODUCT_SERVICE_URL
              value: http://product-service:3000
            - name: MAIL_SERVICE_URL
              value: http://mail-service:3000
---
apiVersion: v1
kind: Service
metadata:
  name: app-service-v3
  labels:
    app: trendtrove-app-v3
spec:
  selector:
    app: trendtrove-app-v3
  ports:
    - name: http
      port: 3000
      targetPort: 3000
